# 导入 Markdown

此功能允许将 Markdown 文档内容导入到 Umo Editor 编辑器中。

## Markdown 语法扩展

### 高亮块

使用 `:::` 来标识高亮块的开始和结束。

```
:::info
高亮块内容
:::
```

## 方法列表

### insertMarkdown

**说明**：插入 Markdown 格式文本。

**参数**：

- `markdown`，String，Markdown 文本。
- `exRules`，object，Markdown 扩展规则。

**返回值**：无