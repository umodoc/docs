import { Badge } from '/components/badge'

# 功能及扩展配置

用于配置 Umo Editor 功能及扩展。自定义扩展请参阅[自定义扩展](../extensions)。

## 默认配置

```js
{
  extensions: [],
  disableExtensions: [], // v8.0.0 新增
}
```

## 配置项说明

### extensions

**说明**：自定义扩展列表。

**类型**：`Array`

**默认值**：`[]`。

**可选值**：合法的 Tiptap 扩展。

### disableExtensions

**说明**：要禁用的功能或扩展列表，禁用后，该功能或扩展将无法使用，菜单中也不会显示。<Badge theme="success">v8.0.0 新增</Badge>

**类型**：`Array`

**默认值**：`[]`。

**可选值**：

Umo Editor：`highlight`, `ordered-list`, `bullet-list`, `task-list`, `line-height`, `margin`, `quote`, `code`, `select-all`, `import-word`, `markdown`, `print`, `link`, `image`, `video`, `audio`, `file`, `code-block`, `symbol`, `chineseDate`, `emoji`, `math`, `tag`, `columns`, `callout`, `mention`, `bookmark`, `hard-break`, `hr`, `toc`, `text-box`, `template`, `web-page`, `qrcode`, `barcode`, `signature`, `seal`, `diagrams`, `echarts`, `mermaid`, `chinese-case`, `watermark`, `background`, `preview`, `export-image`, `export-pdf`, `export-text`, `share`, `embed`, ...

[Umo Editor Next](../next)：`icon`, `import-word`, `import-excel`, `import-ppt`, `import-wps`, `import-markdown`, `import-text`, `export-word`, `export-html`, ...
