import { useRouter } from 'next/router'
import { useEffect } from 'react'

[开发文档](https://editor.umodoc.com/en/docs)。

export default function Home() {
  const router = useRouter()
  useEffect(() => {
    router.push('/en/docs')
  }, [])
  return null
}
